version: '3.9'

services:
  ssh-honeypot:
    container_name: ssh-honeypot
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "2222:2222"
    env_file:
      - .env
    volumes:
      - ssh-honeypot:/assets/keys
    restart: unless-stopped

volumes:
  ssh-honeypot: